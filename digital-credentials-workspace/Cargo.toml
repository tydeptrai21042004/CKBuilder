[workspace]
resolver = "2"
members = [
  # @@INSERTION_POINT@@
  "contracts/credential-revocation",
  "tests",
]

[profile.release]
overflow-checks = true
codegen-units = 1
debug = false
strip = "symbols"
panic = "abort"
