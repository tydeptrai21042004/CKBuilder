# CKBuilder weekly reports

Write one report per week based on work you actually completed. Include commands, screenshots, test output, transaction hashes (when using devnet/testnet), problems, fixes, and the next step.

The files under `legacy-reference/` came from the original starter archive and may identify a different author. They are retained only as formatting references and must not be submitted as your own work.
